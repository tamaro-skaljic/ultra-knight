# UI Asset Pack Decision Helper

- Uses [Spotlight.js](https://github.com/nextapps-de/spotlight).
  > Note: The bundle is changed in one way, namely `2950` is replaced by `1450` and `3E3` is replaced by `1500` for the autohide feature, because [while the docs state that it's possible to configure the autohide feature's timing](https://github.com/nextapps-de/spotlight/blob/master/README.md?plain=1#L492-L497), it isn't.
